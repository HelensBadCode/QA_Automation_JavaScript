const circleRadius = 7.0;
const NUMBER_PI = Math.PI;
const circleAria = NUMBER_PI * circleRadius ** 2;
console.log(
  `The area of the circle by the formula "π * radius ^ 2" has the following result: ${circleAria.toFixed(2)} сm2.`,
);
