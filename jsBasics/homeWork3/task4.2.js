const rectangleLengths = 10;
const rectangleWidth = 12;
const rectangleArea = rectangleLengths * rectangleWidth;
console.log(
  `The area of the rectangle by the formula "length * width" has the following result: ${rectangleArea.toFixed(2)} cm2.`,
);
