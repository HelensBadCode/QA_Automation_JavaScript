const radiusCylinder = 42;
const heightCylinder = 7;
const volumeCylinder = Math.PI * (Math.pow(radiusCylinder,2)) * heightCylinder;
console.log(`The volume of the cylinder by the formula "π * radius^2 * height" has the following result: ${volumeCylinder.toFixed(2)} πr2h.`);
