let circleRadius = 7.00;
const NUMBER_PI = Math.PI;
let circleAria = NUMBER_PI * (Math.pow(circleRadius,2));
console.log(`The area of the circle by the formula "π * radius ^ 2" has the following result: ${circleAria.toFixed(2)} сm2.`);